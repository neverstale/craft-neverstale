{% if craft.app.request.getParam('welcome') %}

    <div class="step step-{{ step }}">
        <div class="number">{{ step }}</div>
        <div class="text">{{ text|t('neverstale') }}</div>
    </div>

{% endif %}
